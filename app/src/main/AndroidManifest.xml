<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.sikdev.diplomankas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ico_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ico_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".User.UserActivity"></activity>
        <activity android:name=".User.UserAuthorizationActivity"></activity>
        <activity android:name=".User.UserRegistrationActivity"></activity>
        <activity android:name=".Info.NoInfoActivity" />
        <activity android:name=".ProductActivity" />
        <activity android:name=".CategoryAndProductActivity" />
        <activity android:name=".BasketActivity" />
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>